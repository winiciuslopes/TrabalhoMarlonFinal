import { IdadeDirective } from './idade.directive';

describe('IdadeDirective', () => {
  it('should create an instance', () => {
    const directive = new IdadeDirective();
    expect(directive).toBeTruthy();
  });
});
