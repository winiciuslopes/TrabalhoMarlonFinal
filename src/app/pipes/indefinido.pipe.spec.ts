import { IndefinidoPipe } from './indefinido.pipe';

describe('IndefinidoPipe', () => {
  it('create an instance', () => {
    const pipe = new IndefinidoPipe();
    expect(pipe).toBeTruthy();
  });
});
